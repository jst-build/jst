// Target definitions
{
  // 'defaults' target is used to set the default shell (needed by shell tests)
  // - uses the rule '//shell:defaults'
  // - inherits shell defaults from base (toolchain)
  defaults: {
    type: @'//shell:defaults',
    base: [@'base//shell:defaults'],
  },
}
